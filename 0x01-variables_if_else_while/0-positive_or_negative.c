#include <stdio.h>
#include <time.h>
/**
 * main - Entry point of my program
 * 
 * Description:  assigning a random number
 * Return: Always 0
 */

 int main(void)
 {
    int a;

    srand(time(0));
	n = rand() - RAND_MAX / 2;
if (number > 0)
{
    printf("Positive\n");
}
else
    {
        printf("Negative\n");
    }
	return (0);
 }