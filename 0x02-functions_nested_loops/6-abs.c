#include "main.h"
/**
 * _abs - computes the absolute value of an integer.
 * @c: the character to be computed
 *
 * Return: Absolte value of number or zero
 */
int _abs(int c)
{
	if (c < 0)
		return (-c);

	return (c);
}
